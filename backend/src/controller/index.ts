import type { Request, Response } from 'express';

/**
 * Example controller - replace with actual controller logic
 */
export function exampleController(_req: Request, res: Response): void {
  res.json({ message: 'Hello from controller' });
}
